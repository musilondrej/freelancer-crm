repos:
  - repo: local
    hooks:
      - id: laravel-sail-lint
        name: Laravel Sail lint (Rector, Pint, PHPStan)
        entry: bash .githooks/pre-commit
        language: system
        pass_filenames: false
        require_serial: true
